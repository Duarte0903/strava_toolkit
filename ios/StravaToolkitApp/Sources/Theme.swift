import SwiftUI

enum Theme {
    static let accent = Color(red: 0xFC / 255, green: 0x52 / 255, blue: 0x00 / 255) // Strava orange
    static let hrLine = accent
    static let paceLine = Color(red: 0x2F / 255, green: 0x6F / 255, blue: 0xED / 255)
    static let routeLine = accent
    static let startDot = Color(red: 0x0E / 255, green: 0x9F / 255, blue: 0x6E / 255)
    static let endDot = Color(red: 0xE0 / 255, green: 0x24 / 255, blue: 0x5E / 255)
}
